mutation ($propertyId: ID!, $name: String!, $color: String!){
    createSidebarFilter(name: $name, propertyId: $propertyId, color: $color){
        id
        name
        order
        property{
            id
        }
        dependentOn{
            id
        }
    }
}
