query (
$instIds: [ID!],
){
  filtersByInstanceIds(instIds: $instIds){
    someFilters{
      id
      property{
        id
      }
    }
    everyFilters{
      id
      property{
        id
      }
    }
  }
}
