query (
$instIds: [ID!],
){
  filtersByInstanceIds(instIds: $instIds){
    id
    someFilters{
      id
      property{
        id
      }
    }
    everyFilters{
      id
      property{
        id
      }
    }
  }
}
